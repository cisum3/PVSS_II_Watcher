# Watch preferences (Watch-PvssLog.ps1)
# Edit values below. Lines starting with # are ignored.
# CLI flags override these when you launch with -Port / -LogPath / etc.
# Invalid values are rejected, logged in the host console, and reset to defaults.
# Edits are picked up by Restart in the dashboard, or the next host start.
# PreferredPort / MaxPortTries / OpenBrowser / Browser are startup-only - those
# need Run-Watch.cmd again, since the host is already listening.
# See package root readMe.txt for a short overview.
#
# On Start, LogPath is updated automatically to the path you used.
# Host is always localhost (127.0.0.1) only.

# TCP port to try first. If busy, tries the next ports.
# PreferredPort: integer 1-65535 (default 8787)
PreferredPort=8787
# MaxPortTries: integer 1-200 (default 40)
MaxPortTries=40

# How often the dashboard polls once loading is done (seconds).
# While a window is loading, the UI polls about every 0.5s for progress %.
# RefreshSeconds: integer 1-60 (default 1)
RefreshSeconds=1

# Open a browser when the host starts.
# OpenBrowser: true|false (default true)
OpenBrowser=true

# Which browser to open.
# Browser: default | chrome | msedge | or full path to an .exe (default default)
Browser=default

# Initial time window when the UI loads (minutes). Ignored if DefaultWindowEntire=true.
# DefaultWindowMinutes: integer 1-10080 (default 60)  [10080 = 7 days]
DefaultWindowMinutes=60
# DefaultWindowEntire: true|false (default false)
DefaultWindowEntire=false

# Initial severity chips (comma-separated). INFO off unless listed.
# DefaultSeverities: FATAL,SEVERE,ERROR,WARNING,INFO (default FATAL,SEVERE,ERROR,WARNING)
DefaultSeverities=FATAL,SEVERE,ERROR,WARNING

# Initial area chips (comma-separated). OTHER = any area not SYS/IMPL/CTRL/PARAM.
# DefaultAreas: SYS,IMPL,CTRL,PARAM,OTHER (default all)
DefaultAreas=SYS,IMPL,CTRL,PARAM,OTHER

# Pattern / table depth.
# TopN: integer 5-100 (default 10)
TopN=10
# SamplePerPattern: integer 1-20 (default 1)
SamplePerPattern=1
# Max characters kept for pattern labels and example log lines (then ...).
# SampleMaxChars: integer 100-2000 (default 500)
SampleMaxChars=500

# BACnet "flapper" threshold (Failed/OK status changes).
# BacFlapMin: integer 1-50 (default 3)
BacFlapMin=3

# Live PVSS_II.log path (prefill). Leave empty until Start, or set your site path.
# LogPath: empty or a file path (updated on Start)
LogPath=c:\Users\Isaac\Documents\Cursor\PVSII_Log\docs\PVSS_II_Examples\PVSS_II_Test.log



